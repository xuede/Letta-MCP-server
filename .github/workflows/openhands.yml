name: OpenHands Resolver
# Test trigger - adding comment
on:
  issues:
    types: [opened, edited]
  issue_comment:
    types: [created, edited]

jobs:
  resolve:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: OpenHands Resolver
        uses: openhands/resolver@v1
        with:
          github-token: ${{ secrets.GITHUB_TOKEN }}
          fix-me-label: 'fix-me'